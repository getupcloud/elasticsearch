cluster:
  name: ${ELASTICSEARCH_CLUSTER_NAME}

discovery:
  zen:
    ping:
      multicast:
        enabled: false

path:
  data: /elasticsearch/persistent/${ELASTICSEARCH_CLUSTER_NAME}/data
  #logs: /elasticsearch/${ELASTICSEARCH_CLUSTER_NAME}/logs
  work: /elasticsearch/${ELASTICSEARCH_CLUSTER_NAME}/work
